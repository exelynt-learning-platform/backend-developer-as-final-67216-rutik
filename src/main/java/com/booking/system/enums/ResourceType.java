package com.booking.system.enums;

/**
 * Broad classification of bookable resources.
 * OTHER is available for anything that doesn't fit the common categories.
 */
public enum ResourceType {
    ROOM,
    VEHICLE,
    EQUIPMENT,
    OTHER
}
